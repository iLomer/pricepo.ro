# Domain Map — Prevo (prevo.ro)

> Generated by `@meto-pm` at swarm init. **READ ONLY after generation.**
> Epic agents use this to check for file conflicts before picking up a task.

---

## Ownership Rules

1. Each epic agent owns its declared domain exclusively
2. Before touching any file, check it is within your domain
3. If a task requires a file outside your domain — STOP, log conflict in `SWARM_AWARENESS.md`, wait for user
4. Shared config files (e.g. `package.json`, `tsconfig.json`) require user coordination before editing

---

## Epic Domains

| Epic ID | Epic Name | Owns | Shared With |
|---|---|---|---|
| E1 | Project Setup | `/src/lib/`, `/src/config/`, root config files | E2 (Supabase client) |
| E2 | Auth & Fiscal Profile | `/src/app/(auth)/`, `/src/app/onboarding/`, `/src/components/auth/`, `/src/components/onboarding/`, `/supabase/migrations/*auth*`, `/supabase/migrations/*profile*` | E1 (Supabase client) |
| E3 | Core PFA Features | `/src/app/(dashboard)/`, `/src/components/calendar/`, `/src/components/estimator/`, `/src/components/d212/`, `/src/components/alerts/`, `/src/lib/fiscal/` | E2 (fiscal profile data) |
| E4 | Landing Page & Validation | `/src/app/(marketing)/`, `/src/components/landing/`, `/src/components/waitlist/` | none |
| E5 | Deploy & Production | `/vercel.json`, `/.github/`, deployment configs | E1 (env vars) |
| E6 | ANAF Integration | `/src/lib/anaf/`, `/src/app/api/anaf/`, `/src/components/anaf/` | E2 (auth tokens), E3 (e-Factura data read-only) |
| E7 | SRL Features | `/src/app/(dashboard)/srl/`, `/src/components/srl/`, `/src/lib/fiscal/srl/` | E2 (fiscal profile), E3 (calendar) |

**Note:** E8 (CAEN Expense Deductibility Guide) has been DROPPED per D008. No domain assignment.

---

## Shared Files (requires coordination)

Files that multiple epics may need to touch. Epic agents must flag before editing.

| File | Reason shared | Protocol |
|---|---|---|
| `package.json` | All epics may add deps | Flag in SWARM_AWARENESS, user approves |
| `tsconfig.json` | Compiler config affects all | Flag in SWARM_AWARENESS, user approves |
| `CLAUDE.md` | Project-wide context | Only @meto-pm writes |
| `/src/lib/supabase.ts` | Client used by E1, E2, E3, E6 | E1 creates, others read-only |
| `/src/app/layout.tsx` | Root layout affects all pages | Flag in SWARM_AWARENESS, user approves |
| `tailwind.config.ts` | Theme/design tokens | E1 creates, flag for changes |

---

## Dropped Routes

The following routes have been removed from the product (D008 -- No Bookkeeping):
- `/registru` -- transaction ledger, dropped entirely

---

## Conflict Resolution Protocol

1. Epic agent detects file outside its domain is needed
2. Agent logs conflict in `SWARM_AWARENESS.md`
3. Agent pauses — does NOT proceed
4. User reviews and assigns ownership or approves shared edit
5. Agent continues only after user confirmation
